var FirmataPi = require('../firmata-pi');
var Board = require('firmata').Board;
var sinon = require('sinon');

module.exports['Works with Firmata Board'] = function (test) {
  // var firmataPi = new FirmataPi();
  // // test.expect(1);

  // var board = new Board(firmataPi, function (error) {
  //   test.equal(error, undefined, 'Firmata started without erorr');
  //   // keepTesting();
  // });

  test.done();
};